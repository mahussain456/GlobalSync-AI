{
  "dependencies": {
    "@craco/craco": "^7.1.0",
    "@vercel/og": "0.11.1"
  },
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install --legacy-peer-deps",
    "build": "cd frontend && npm run build"
  }
}
